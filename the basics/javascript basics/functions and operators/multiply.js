// Take two inputs in our product function and multiply them together using the * operator. Return this value.
function product(a,b) {
    return a*b;
}
const a = 3;
const b = 4;
const out = product(a,b);
console.log(out);
module.exports = product;